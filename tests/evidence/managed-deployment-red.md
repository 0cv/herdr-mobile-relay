# Managed deployment red evidence

- `2026-09-06`: `go test ./internal/profiles -run 'TestCustomPaneAssociations|TestAssociation' -count=1` failed to compile because `Observation`, `WithAssociationStore`, `Reconcile`, and the persisted association store did not exist. This freezes the required restart-safe, fail-closed custom profile ownership contract before production implementation.
- `2026-09-06`: `go test ./internal/coordinator -run TestUnknownCustomProfileOwnershipDeniesPaneDestruction -count=1` failed to compile because the stable unknown-ownership denial did not exist. This freezes denial of stop, clear, and restart before any Herdr subprocess or pane-state mutation.
- `2026-09-06`: `go test ./internal/config ./internal/app -run 'TestLoadManagedDeployment|TestManagedDeploymentOmitsAndRejectsRemoteUpdates' -count=1` failed to compile because managed deployment configuration and its stable protocol error did not exist. This freezes both handshake capability removal and direct install/deploy rejection at the live WebSocket boundary.
- `2026-09-06`: `go test ./internal/readiness -count=1` failed to compile because exact generation-bound inventory readiness did not exist. This freezes exact pane/native-session/profile matching, distinct acknowledged-empty state, missing/corrupt/newer/duplicate rejection, and unexpected-empty/partial/mismatch failures.
- `2026-09-06`: `go test ./internal/config ./internal/app -run 'TestLoadManagedDeployment|TestManaged(Readiness|Deployment)' -count=1` failed to compile because the managed expected-inventory path and active generation were not wired into the server. This freezes `/readyz` fail-closed behavior and preserves `acknowledged_empty` as a distinct `/readyz` and `/healthz` state.
- `2026-09-06`: `bash tests/test_credential_hygiene.sh` failed with `relay setup accepted ambient GitHub credentials`. This freezes fail-before-write behavior for ambient `GH_TOKEN`, `GITHUB_TOKEN`, and `HERDR_GITHUB_TOKEN_FILE`, plus a clean tokenless setup with no GitHub credential pointer/file.
- `2026-09-06`: `go test ./internal/profiles -run 'Test(PresentUnverified|MismatchedLive|AssociationPrunesOnly)' -count=1` failed because an empty native ID rejected a still-live pane, a mismatched native ID erased its durable record, and explicit `Forget` was rehydrated from the store. This freezes the reviewed continuity rule: empty or mismatched live observations remain unknown without erasing the last exact tuple; only definitive absence or explicit lifecycle close prunes it; the exact tuple can later revalidate.
- `2026-09-06`: `go test ./internal/supervisor -count=1` failed to compile because no explicit child environment policy, bounded pair supervisor, persistent trip/reset state, capped backoff, health timeout, or rotating writer existed. This freezes secret-free disjoint Relay/cloudflared children, exact retry caps, no child-pair overlap, cancellation cleanup, visible tripping, and bounded stdout/stderr before production implementation.
- `2026-09-06`: `go test ./internal/supervisor -run 'TestJointReadiness|TestActiveConfig' -count=1` failed to compile because the supervisor exposed only one local health URL and snapshotted Relay socket/generation values from its own startup environment. This freezes two gates before production implementation: local-green/public-red must never reach `running`, and an atomically promoted active config must restart the child pair with socket, generation, and expected inventory all taken from one validated snapshot so no mixed generation can be exposed.
- `2026-09-06`: `go test ./internal/supervisor -run 'TestRunStateMachine|TestRunPairPropagates' -count=1` failed to compile because deterministic state, cycle, and bounded-log seams did not exist. This freezes propagation of every durable-state boundary, stable-only failure reset, reload/backoff cancellation, all four child-log open failures, and both child start failures before the testability refactor.
- `2026-09-06`: `bash tests/test_credential_hygiene.sh` exited 1 after the legacy-token symlink assertion because `ensure_relay_env` removed only a regular `github-token` file and left a symlink behind. This freezes unlinking the legacy path without following it, preserving the symlink target, and accepting an already-absent path.
